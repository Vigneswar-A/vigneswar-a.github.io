SELECT ROUND(SUM(CASE WHEN D1.ORDER_DATE = D1.CUSTOMER_PREF_DELIVERY_DATE THEN 1 ELSE 0 END)/COUNT(*)*100, 2) AS IMMEDIATE_PERCENTAGE FROM DELIVERY D1 WHERE D1.ORDER_DATE IN (SELECT MIN(D2.ORDER_DATE) FROM DELIVERY D2 WHERE D2.CUSTOMER_ID = D1.CUSTOMER_ID)