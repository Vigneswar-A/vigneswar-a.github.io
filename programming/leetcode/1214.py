SELECT e1.STUDENT_ID , MIN(e1.COURSE_ID) as course_id, e1.grade AS GRADE FROM ENROLLMENTS E1 WHERE GRADE = (SELECT MAX(GRADE) FROM ENROLLMENTS e WHERE e.STUDENT_ID = E1.STUDENT_ID GROUP BY e.STUDENT_ID) GROUP BY E1.STUDENT_ID, e1.grade order by e1.student_id
