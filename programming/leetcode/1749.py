SELECT SELLER.seller_name from (SELLER LEFT JOIN ORDERS ON SELLER.SELLER_ID = ORDERS.SELLER_ID)  GROUP BY SELLER.SELLER_id, seller_name HAVING SUM(CASE WHEN TO_CHAR(SALE_DATE, 'YYYY') = '2020' THEN 1 ELSE 0 END) = 0 ORDER BY SELLER.SELLER_NAME 