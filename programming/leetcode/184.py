SELECT d1.NAME AS DEPARTMENT, EMPLOYEE.NAME AS EMPLOYEE, SALARY FROM (EMPLOYEE JOIN DEPARTMENT d1 ON EMPLOYEE.DEPARTMENTID = d1.ID) WHERE SALARY = (SELECT max(SALARY) FROM (EMPLOYEE JOIN DEPARTMENT d ON EMPLOYEE.DEPARTMENTID = d.ID) where d1.name = d.name)